-- Confirmación de esquema para Tarjetas Digitales
-- El esquema actual en docs/acloud.txt es correcto y suficiente.
-- La columna 'config' (JSONB) maneja dinámicamente el switch de idioma y nuevos campos sin cambios en la base de datos.

-- Solo para referencia, el esquema es:
-- CREATE TABLE IF NOT EXISTS digital_cards (
--     id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
--     user_id UUID REFERENCES directory_users(id) ON DELETE CASCADE UNIQUE,
--     config JSONB DEFAULT '{}'::jsonb,
--     created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
-- );
